1. I am using mariadb 10.3.38 and Node version is 19.8.1
2 First you need to run command npm install in root directory and client directory
3 check .env file on server directory and change the DB credentials.
4 table.sql have table schema and data.
5 run npm start on root directory.

